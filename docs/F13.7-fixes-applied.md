# F13.7 — Correções Aplicadas (MEDIUM + LOW)

## BUG-13 — Middleware deprecado → proxy.ts (MEDIUM)
- **Arquivo:** `src/proxy.ts` (criado), `src/middleware.ts` (removido)
- Renomeado `export default function middleware` → `export async function proxy`
- Next.js 16 usa a convenção `proxy.ts` — o aviso de build foi eliminado

## BUG-14 — R2 client inicializado no nível do módulo (MEDIUM)
- **Arquivo:** `src/lib/r2.ts`
- S3Client movido para função `getR2Client()` com validação das 3 variáveis obrigatórias
- Crash em rotas sem R2 configurado foi eliminado

## BUG-15 — Incremento de views não atômico (MEDIUM)
- **Arquivo:** `src/app/api/forum/topics/[id]/route.ts`
- Substituído SELECT + UPDATE por `supabase.rpc("increment_topic_views", { topic_id })`
- **DDL necessário:**
```sql
CREATE OR REPLACE FUNCTION increment_topic_views(topic_id uuid)
RETURNS void AS $$
  UPDATE forum_topics SET views_count = views_count + 1 WHERE id = topic_id;
$$ LANGUAGE sql;
```

## BUG-16 — BreedImage sem timeout na API externa (MEDIUM)
- **Arquivo:** `src/components/BreedImage.tsx`
- Adicionado `AbortController` com timeout de 5 segundos no fetch para `dog.ceo`

## BUG-17 — Approve/reject sem auth server-side (MEDIUM)
- **Arquivos criados:**
  - `src/app/api/admin/approve/route.ts` — POST com verificação de auth + role (approver/super_admin)
  - `src/app/api/admin/reject/route.ts` — POST com verificação de auth + role + reason obrigatório
- **Arquivo modificado:** `src/app/admin/aprovar/page.tsx`
  - `handleApprove` e `handleReject` agora chamam as rotas API via fetch

## BUG-18 — approver/super_admin veem dashboard de client (MEDIUM)
- **Arquivo:** `src/app/painel/layout.tsx`
- `isApprover` adicionado à desestruturação de `useRole()`
- `navItems` e cabeçalho "Painel do Criador" agora usam `(isKennel || isApprover)`

## BUG-19 — Race condition nos contadores de likes (LOW)
- **Arquivos:** `src/app/api/posts/[id]/like/route.ts`, `src/app/api/forum/topics/[id]/like/route.ts`, `src/app/api/forum/replies/[id]/like/route.ts`
- Padrão SELECT + UPDATE (race condition) substituído por RPCs atômicos
- **DDL necessário:**
```sql
CREATE OR REPLACE FUNCTION toggle_post_like(p_post_id uuid, p_user_id uuid)
RETURNS TABLE(liked boolean, likes_count bigint) AS $$
DECLARE v_liked boolean; v_count bigint;
BEGIN
  IF EXISTS (SELECT 1 FROM post_likes WHERE post_id = p_post_id AND user_id = p_user_id) THEN
    DELETE FROM post_likes WHERE post_id = p_post_id AND user_id = p_user_id;
    v_liked := false;
  ELSE
    INSERT INTO post_likes (post_id, user_id) VALUES (p_post_id, p_user_id);
    v_liked := true;
  END IF;
  SELECT COUNT(*) INTO v_count FROM post_likes WHERE post_id = p_post_id;
  UPDATE posts SET likes_count = v_count WHERE id = p_post_id;
  RETURN QUERY SELECT v_liked, v_count;
END;
$$ LANGUAGE plpgsql;

CREATE OR REPLACE FUNCTION toggle_topic_like(p_topic_id uuid, p_user_id uuid)
RETURNS TABLE(liked boolean, likes_count bigint) AS $$
DECLARE v_liked boolean; v_count bigint;
BEGIN
  IF EXISTS (SELECT 1 FROM forum_topic_likes WHERE topic_id = p_topic_id AND user_id = p_user_id) THEN
    DELETE FROM forum_topic_likes WHERE topic_id = p_topic_id AND user_id = p_user_id;
    v_liked := false;
  ELSE
    INSERT INTO forum_topic_likes (topic_id, user_id) VALUES (p_topic_id, p_user_id);
    v_liked := true;
  END IF;
  SELECT COUNT(*) INTO v_count FROM forum_topic_likes WHERE topic_id = p_topic_id;
  UPDATE forum_topics SET likes_count = v_count WHERE id = p_topic_id;
  RETURN QUERY SELECT v_liked, v_count;
END;
$$ LANGUAGE plpgsql;

CREATE OR REPLACE FUNCTION toggle_reply_like(p_reply_id uuid, p_user_id uuid)
RETURNS TABLE(liked boolean, likes_count bigint) AS $$
DECLARE v_liked boolean; v_count bigint;
BEGIN
  IF EXISTS (SELECT 1 FROM forum_reply_likes WHERE reply_id = p_reply_id AND user_id = p_user_id) THEN
    DELETE FROM forum_reply_likes WHERE reply_id = p_reply_id AND user_id = p_user_id;
    v_liked := false;
  ELSE
    INSERT INTO forum_reply_likes (reply_id, user_id) VALUES (p_reply_id, p_user_id);
    v_liked := true;
  END IF;
  SELECT COUNT(*) INTO v_count FROM forum_reply_likes WHERE reply_id = p_reply_id;
  UPDATE forum_replies SET likes_count = v_count WHERE id = p_reply_id;
  RETURN QUERY SELECT v_liked, v_count;
END;
$$ LANGUAGE plpgsql;
```

## BUG-20 — PostCard sem tratamento de erro em like/delete (LOW)
- **Arquivo:** `src/components/community/PostCard.tsx`
- `handleLike`: reverte atualização otimista se `res.ok === false`
- `handleDelete`: não chama `onDelete` se `res.ok === false`

## BUG-21 — APIs com `{ message }` em vez de `{ error }` (LOW)
- Nenhuma ocorrência encontrada. Todas as rotas já usam `{ error: ... }` corretamente.

## BUG-22 — URL do Instagram hardcoded (LOW)
- **Arquivos:** `src/components/layout/Footer.tsx`, `src/app/page.tsx`, `src/app/comunidade/page.tsx`
- Substituído `"https://instagram.com/fareja"` por `process.env.NEXT_PUBLIC_INSTAGRAM_URL ?? "https://instagram.com/fareja"`
- **Arquivo criado:** `.env.example` com todas as variáveis de ambiente documentadas

## BUG-23 — useFavorites sem estado de erro (LOW)
- **Arquivo:** `src/lib/hooks/useFavorites.ts`
- Adicionado `error: string | null` ao estado e retorno do hook
- `setError(err.message)` chamado quando a query falha

## BUG-24 — callbackUrl sem fallback para NEXT_PUBLIC_SITE_URL (LOW)
- **Arquivo:** `src/app/(auth)/login/page.tsx`
- `callbackUrl` agora usa `process.env.NEXT_PUBLIC_SITE_URL` quando disponível (preferencial em produção)
- Fallback para `window.location.origin` mantido para desenvolvimento

## BUG-25 — Autocomplete de raças no formulário de filhotes (LOW)
- **Arquivo:** `src/app/painel/filhotes/page.tsx`
- Estado `breedSuggestions` adicionado
- Fetch da tabela `breeds` (coluna `name_pt`) adicionado no `load()`
- `<datalist id="breeds-list">` renderizado quando há sugestões
- Campo de raça usa `list="breeds-list"`

## BUG-26 — Financeiro com dados mockados (LOW)
- **Arquivo:** `src/app/admin/financeiro/page.tsx`
- Todos os dados hardcoded removidos
- Exibe placeholder "Em desenvolvimento" com ícone e mensagem descritiva
- Controle de acesso (super_admin) e navegação admin mantidos

---

## Build
```
✓ Compiled successfully in 4.2s
✓ 37 pages — zero erros, zero warnings
```
